module: todo-api
assumptions:
  - id: postgresql_storage
    category: infrastructure
    content: "PostgreSQL storage with uuid primary keys"

  - id: jwt_auth
    category: security
    content: "All endpoints require valid JWT via user-auth.verify_token"

  - id: tags_storage
    category: data
    content: "Tags stored as JSONB array column"

  - id: user_limit
    category: data
    content: "Max 10,000 todos per user"
